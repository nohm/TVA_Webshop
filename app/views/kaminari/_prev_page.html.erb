<li>
  <% if current_page.first? %>
  	<%= link_to raw(t 'views.pagination.previous'), url, :rel => 'prev', :remote => remote, style: 'pointer-events: none' %>
  <% else %>
		<%= link_to raw(t 'views.pagination.previous'), url, :rel => 'prev', :remote => remote %>
  <% end %>
</li>
