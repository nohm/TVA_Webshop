<li>
	<% if current_page.last? %>
  	<%= link_to raw(t 'views.pagination.last'), url, {:remote => remote, style: 'pointer-events: none;'} %>
  <% else %>
		<%= link_to raw(t 'views.pagination.last'), url, {:remote => remote} %>
  <% end %>
</li>
