<li>
  <% if current_page.last? %>
  	<%= link_to raw(t 'views.pagination.next'), url, :rel => 'next', :remote => remote, style: 'pointer-events: none;'  %>
  <% else %>
		<%= link_to raw(t 'views.pagination.next'), url, :rel => 'next', :remote => remote %>
  <% end %>
</li>
