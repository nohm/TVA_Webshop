<li>
  <% if current_page.first? %>
  	<%= link_to raw(t 'views.pagination.first'), url, {:remote => remote, style: 'pointer-events: none;'} %>
  <% else %>
		<%= link_to raw(t 'views.pagination.first'), url, :remote => remote %>
  <% end %>
</li>
